@model IEnumerable<RankPrediction_Web.Models.PredictionDatum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KillDeathRatio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AverageDamage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MatchCounts)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsParty)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreateDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rank)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Season)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KillDeathRatio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AverageDamage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MatchCounts)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsParty)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreateDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rank.RankName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Season.SeasonName1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
