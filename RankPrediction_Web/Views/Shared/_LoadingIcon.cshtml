
<style>

    .loader {
        position: relative;
        top: 50%;
        left: 50%;
        width: 128px;
        height: 128px;
        transform: translate(-50%);
    }

        .loader div {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            box-sizing: border-box;
            opacity: .8;
        }

    .one {
        border-top: 4px solid #8FB259;
        animation: rotate-left 1s linear infinite;
    }

    .two {
        border-right: 4px solid #CCCC52;
        animation: rotate-right 1s linear infinite;
    }

    .three {
        border-bottom: 3px solid #FFD933;
        animation: rotate-right 1s linear infinite;
    }

    .four {
        border-left: 4px solid #FF7F00;
        animation: rotate-right 1s linear infinite;
    }

    @@keyframes rotate-left {
        0% {
            transform: rotate(360deg);
        }

        100% {
            transform: rotate(0deg);
        }
    }

    @@keyframes rotate-right {
        0% {
            transform: rotate(0deg);
        }

        100% {
            transform: rotate(360deg);
        }
    }
</style>

<div id="load-icon-div" class="loader">
    <div class="one"></div>
    <div class="two"></div>
    <div class="three"></div>
    <div class="four"></div>
</div>
